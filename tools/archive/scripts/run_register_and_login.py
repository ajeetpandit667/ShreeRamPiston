"""
Developer helper removed.

This file previously contained local smoke-test scripts. Those helpers have been removed
from the repository root to avoid cluttering the project. If you need to keep them,
place test helpers under a dedicated `tools/` or `tests/` folder and exclude them from
production commits.
"""
